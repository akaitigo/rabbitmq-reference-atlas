interface:
  display_name: "RabbitMQ Reference Router"
  short_description: "RabbitMQ 4.3.5の実証済み設計・診断・復旧へ案内"
  default_prompt: "Use $rabbitmq-reference-router to review this RabbitMQ design against the fixed atlas evidence."
policy:
  allow_implicit_invocation: true

