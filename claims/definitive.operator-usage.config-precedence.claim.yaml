schema_version: 1
id: definitive.operator-usage.config-precedence.claim
atlas_id: rabbitmq-reference-atlas
capability_id: operator.cluster
statement: Operator defaults and user RabbitMQ configuration precedenceのRabbitMQ 4.3.5における公開契約は、Authorityから導出した各必須Scenarioの実行結果と一致する。
status: proposed
source_ids:
- rabbitmq-operator-usage
proof_obligations:
- id: proof.operator-usage.config-precedence.normal
  statement: Operator defaults and user RabbitMQ configuration precedenceのnormal Scenarioを専用実Runtime
    Evidenceで証明する。
  acceptance_criteria:
  - 固定Versionの実Runtimeで正常入力を実行し、期待Outcomeを構造化Artifactで判定する。
- id: proof.operator-usage.config-precedence.boundary
  statement: Operator defaults and user RabbitMQ configuration precedenceのboundary
    Scenarioを専用実Runtime Evidenceで証明する。
  acceptance_criteria:
  - 上限、下限、空、重複または優先順位の境界を一つずつ変え、反証可能なOracleで判定する。
- id: proof.operator-usage.config-precedence.rejection
  statement: Operator defaults and user RabbitMQ configuration precedenceのrejection
    Scenarioを専用実Runtime Evidenceで証明する。
  acceptance_criteria:
  - 無効な設定、Credential、Protocolまたは操作を与え、部分成功を認めず明示的な拒否を確認する。
- id: proof.operator-usage.config-precedence.compatibility
  statement: Operator defaults and user RabbitMQ configuration precedenceのcompatibility
    Scenarioを専用実Runtime Evidenceで証明する。
  acceptance_criteria:
  - 二つ以上のProtocol、Client、Queue、PluginまたはVersion Variantを同一Oracleで比較する。
